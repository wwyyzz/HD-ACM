#-----------输入不定多组数据
#include <stdio.h>

int main() 
{

   int a, b;
   while(scanf("%d%d", &a, &b) != EOF)
      {
        printf("%d\n", a + b);
      }
}



#-----------输入固定多组数据T
#include <stdio.h>

int main() 
{

 while(scanf("%d", &T) != EOF)
	{
	for (i = 1; i <= T ; i++)
     {
       pass()
     }

	}
}